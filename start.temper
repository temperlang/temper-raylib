
let {Raylib} = import("./rl/functions.temper");

export let start(rl: Raylib): Void {
  rl.InitWindow(640, 480, "Hello From Temper");
  while (!rl.WindowShouldClose()) {
    rl.BeginDrawing();
    rl.EndDrawing();
  }
}
