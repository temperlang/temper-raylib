
let {...} = import('./rl/raylib.temper');

InitWindow(640, 480, "Hello From Temper");
while (!WindowShouldClose()) {
    BeginDrawing();
    ClearBackground(RAYWHITE);
    DrawText("Hello Temper!", 20, 20, 64, RED);
    EndDrawing();
}
